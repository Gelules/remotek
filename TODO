Now:
* Begin the thread in a 'begin' function and not directly to the server code
* Use a buffer to send the data in one time only
* Use the cleanup function from UMH to delete the output and stderr files

Features:
* Encrypt data over the network
* Add a password protection
* Hide my module and its presence on the system to not bother the sysadmin
* Make the module persistent after reboot
* Upload and download files
